# Session Replication Test APP (with Tomcat Embedded)

## Usage

Run `mvn clean install` to generate über-jar, and execute with
`java -jar ./target/session-test-1.0-SNAPSHOT-jar-with-dependencies.jar`
